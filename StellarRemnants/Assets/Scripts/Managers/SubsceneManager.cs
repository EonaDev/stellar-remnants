using UnityEngine;

namespace StellarRemnants.Core {
    public class SubsceneManager : MonoBehaviour {
    // TODO: This class will manage the position of all "interior" spaces, like ship/station/building interiors.
        float BlockSize = 200f; // Each block is n^3 meters of space.
        int BlockRange = 25; // Number of blocks in each direction. Resulting in (range*2)^3 total blocks. 25 -> 125,000
    
    }
}
